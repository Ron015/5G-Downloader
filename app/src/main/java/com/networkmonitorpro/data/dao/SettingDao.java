package com.networkmonitorpro.data.dao; import androidx.lifecycle.LiveData; import androidx.room.*; import com.networkmonitorpro.data.entity.Setting; import java.util.*; @Dao public interface SettingDao{ @Query("SELECT * FROM Setting") LiveData<List<Setting>> all(); @Query("SELECT value FROM Setting WHERE `key`=:key LIMIT 1") String get(String key); @Insert(onConflict=OnConflictStrategy.REPLACE) void put(Setting s); }
